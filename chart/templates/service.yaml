kind: Service
apiVersion: v1
metadata:
  name: oceance-kiki-lea
spec:
  selector:
    app: configmap-appsettings-api-oceance-kiki-lea
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 80